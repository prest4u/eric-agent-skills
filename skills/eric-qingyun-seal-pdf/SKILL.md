---
name: eric-qingyun-seal-pdf
description: "【朱印砂卷】青云选科指导报告 Typst 皮：砂褐纸、隶书题+楷体文、四周 6mm 细框、封面框内朱印「青云」。适合家庭留存；客户可见只写青云未来。"
---

# 青云 · 选科指导报告（内部皮：朱印砂卷）

产品只写 **青云未来**（版式美术元素可保留「青云」二字）。本 skill 是 wave-2 皮 F，**不是 A 雾蓝白或已淘汰 B 的换色**，禁止复用 `rail-head`、38mm 边注、9mm 顶通栏、题签。内部名「朱印砂卷」只出现在本文件与 `visual-contract.md`，**不得印上 PDF**。

先读本文件，再读 `references/visual-contract.md`，再 `#import "theme.typ"`。色值、字栈、6mm 细框、框内朱印只许按合同。

朱印是工作室 **朱文方印「青云」**，不是公章，不是国徽，不要考试院口吻。

## 何时用 / 不用

**用：** 家庭留存、要盖章感的选科指导报告。砂褐纸 + 隶书题 + 四周细框 + 框内朱印。本季文种是选科，不要脚手架志愿方案。

**不用 / 改走别的皮：**

| 需求 | 走哪条 |
|---|---|
| 整套客户 PDF 基线：方案、签发、清单、咨询栏轨 | `$eric-qingyun-pdf`（A 雾蓝白，**不要改那条皮**） |
| 售前说明、家长讲解、教师转介 | `$eric-qingyun-parent-pdf`（家长说明册，楷题仿宋 + 38mm 边注） |
| 确认书、签发单、出分后密意见书 | C `$eric-qingyun-cold-ink-pdf` |
| 封面/页眉要让「青云」被认出来 | D `$eric-qingyun-teal-pdf`（浅青识别册） |
| 选科长文、来源页、爱读书的家长 | E `$eric-qingyun-editorial-pdf` |
| 出分后志愿冲稳保表、填报清单 | `$eric-qingyun-plan-pdf` / `$eric-qingyun-checklist-pdf` |
| 三人经营讨论稿、渠道返佣合同 | 不是这个家族 |

## 四硬差（每轴都换）

1. **纸**：砂褐 `#C4A882`。不是 A `#F4F5F3`，不是 B `#F6F3EA`，不是 C `#F3F3F1`，不是 D `#DDE8E4`，不是 E `#E6E4DE`，不是家长说明册 `#EFE4D0`，不是近白。
2. **字**：标题用隶书 `Baoli SC`（回退 `Libian SC`）；正文用楷体 `Kaiti SC`（回退 `STKaiti`）。不要楷题+仿宋，不要全苹方，不要宋体章节号。
3. **网格**：四周距裁切 **6mm 连续细框**，每页四边不断。表只有行发丝：`stroke: (x: none, y: hair)`。禁止左 16mm 栏轨、38mm 边注、9mm 顶通栏、断轨。
4. **封面**：左齐大隶书「选科指导报告」+ 框内右上朱印「青云」。禁止 DOCUMENT/STATUS 底栏，禁止题签，禁止反白顶通栏。

封面无页眉页脚。内页页脚：`青云` · `非正式官方文件 · 不保证录取` · 页码 `01`。

## 输入

最低输入（与共享案例字段对齐，见 `samples/facts.typ`）：

- 化名、省、年、批次（写「高一选科」，不要写本科批）
- 案例号、版本、日期
- 现行组合 / 备选组合 / 硬约束 / 软偏好 / 待补
- 科目门对照行（工科 / 医学 / 经管 / 放弃代价）
- 免责四句原文

没有当年官方来源的条目，标「待核验」，不得写成已核实事实。冲/稳/保仅出现在志愿方案；本册禁止院校冲稳保表。可以说「不开冲稳保」。

## 怎么编译

需要 Typst 0.14.x，并确保 `typst` 在 `PATH` 中可用。

编本 skill 自带 4 页小样：

```bash
python3 scripts/compile_sample.py
```

编小样时脚本会带 `--root <skill-dir>`（小样 `#import "/theme.typ"`）。新项目把 `theme.typ` 拷到稿件目录后：

```bash
typst compile document.typ document.pdf
```

新稿不要写进 skill 包。`#import "theme.typ": *`，`#show: seal-doc.with(...)`（`qingyun-doc` 是别名）。封面用 `cover-page`，门表用 `gate-table`（只有横 hairline，无填色、无勾选框）。朱印用 `studio-seal`，用 `place` 放进框内，不要撑破细框。

自检：

```bash
python3 scripts/self_check.py
```

也可对任意 PDF：`python3 scripts/self_check.py --pdf <file.pdf>`。

目检页图（可选）：

```bash
pdftoppm -png -r 140 samples/选科指导报告-小样.pdf samples/page
```

## 禁词与文种边界

客户可见品牌只写「青云未来」。禁止出现：青云知路、青云志愿。

内部名「朱印砂卷」禁止出现在封面、页眉、页脚、正文、页码旁。

本季文种是选科。禁止做成志愿冲稳保院校表。正文可以说「不开冲稳保」，但不要出现院校冲/稳/保分列。

禁止：录取概率、上岸率、保证上岸、一定录取、公章、国徽、真名、DOCUMENT、STATUS。

朱印只表示工作室署名，不是行政机关公章，不要写考试院套话（免责四句里的「不是教育考试院……官方文件」除外）。

每份客户稿必须有：署名「青云未来」、页脚「非正式官方文件 · 不保证录取」、免责四句、事实 / 判断 / 待核验。

免责四句原文锁定：

1. 本文件不是教育考试院或高校官方文件。
2. 本文件不构成录取、就业或薪资承诺。
3. 最终以当年官方系统、招生计划和高校招生章程为准。
4. 过期、缺失或相互冲突的数据，不得当作已核实事实。

客户文件名风格：`选科指导-案例合成TJ2026-0042-天津2026-V1-20260819.pdf`。小样用 `选科指导报告-小样.pdf`。

## 完成门槛

- A4 竖、正好 4 页（本季小样锁 4 页）
- 砂褐纸、隶书题+楷体文、6mm 连续细框、封面框内朱印与 `visual-contract.md` 一致
- `self_check.py` 通过；`pdftotext` 无禁词，无内部名泄漏
- 可选中文；前两屏见「青云」和「不保证录取」
- 朱印在框内，细框四边不断

## 本切片没做的

九套场景骨架、签发工作流、雾蓝白互编译、可勾选清单、志愿方案模块。不要回写 `$eric-qingyun-pdf`、`$eric-qingyun-teal-pdf`、`$eric-qingyun-parent-pdf` 或其他青云皮。
